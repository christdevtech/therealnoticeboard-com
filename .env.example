# Added by Payload
PREVIEW_SECRET=YOUR_SECRET_HERE
# Used to validate preview requests
CRON_SECRET=YOUR_CRON_SECRET_HERE
# Secret used to authenticate cron jobs
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
# Used to configure CORS, format links and more. No trailing slash
PAYLOAD_SECRET=7170f65c8a68363eb7ceb940
# Used to encrypt JWT tokens
#DATABASE_URI=postgresql://127.0.0.1:5432/your-database-name
# Or use a PG connection string
DATABASE_URI=mongodb://localhost:27020/thenoticeboard
# Database connection string

# Mapbox configuration for property location mapping
NEXT_PUBLIC_MAPBOX_ACCESS_TOKEN=your_mapbox_access_token_here
# Get your access token from https://account.mapbox.com/access-tokens/

# Optional: Mapbox style URL (defaults to streets-v12 if not provided)
NEXT_PUBLIC_MAPBOX_STYLE=mapbox://styles/mapbox/streets-v12
# Custom map style URL